# API URL (optional - defaults to same host)
# For local development with backend on different port:
# VITE_API_URL=http://localhost:8000

# In production, leave this blank - API will be on same host via ALB routing
VITE_API_URL=
